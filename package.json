{
  "name": "help-me-clean",
  "private": true,
  "type": "module",
  "description": "Point & Tidy — an app that teaches a child to tidy up through the camera. Static, no build step; package.json exists only to run the pure-module tests.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "npx serve ."
  }
}
